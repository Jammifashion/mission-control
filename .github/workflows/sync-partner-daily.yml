name: Partner Sync täglich

on:
  schedule:
    - cron: '0 3 * * *'   # täglich 03:00 UTC
  workflow_dispatch:        # manueller Trigger über GitHub UI

jobs:
  sync:
    name: Trigger Partner-Sync (sync-all)
    runs-on: ubuntu-latest

    steps:
      - name: POST /api/partner/verkaeufe/sync-all (jfn)
        env:
          API_URL: ${{ secrets.BACKEND_URL }}
          API_KEY: ${{ secrets.MC_API_KEY }}
        run: |
          set -e
          if [ -z "$API_URL" ] || [ -z "$API_KEY" ]; then
            echo "::error::BACKEND_URL oder MC_API_KEY fehlt in GitHub Secrets."
            exit 1
          fi
          echo "Triggere Partner-Sync (jfn) gegen $API_URL"
          response=$(curl -sS -w "\n%{http_code}" -X POST \
            -H "x-api-key: $API_KEY" \
            "$API_URL/api/partner/verkaeufe/sync-all?shop=jfn")
          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)
          echo "HTTP $status"
          echo "$body" | jq . || echo "$body"
          if [ "$status" != "200" ]; then
            echo "::error::Sync (jfn) fehlgeschlagen mit HTTP $status"
            exit 1
          fi

      - name: POST /api/partner/verkaeufe/sync-all (honk)
        env:
          API_URL: ${{ secrets.BACKEND_URL }}
          API_KEY: ${{ secrets.MC_API_KEY }}
        run: |
          set -e
          echo "Triggere Partner-Sync (honk) gegen $API_URL"
          response=$(curl -sS -w "\n%{http_code}" -X POST \
            -H "x-api-key: $API_KEY" \
            "$API_URL/api/partner/verkaeufe/sync-all?shop=honk")
          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n1)
          echo "HTTP $status"
          echo "$body" | jq . || echo "$body"
          if [ "$status" != "200" ]; then
            echo "::error::Sync (honk) fehlgeschlagen mit HTTP $status"
            exit 1
          fi
